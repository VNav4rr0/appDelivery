import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    height: 52,
    backgroundColor: '#FFF',
    borderRadius: 12,
    borderWidth: 2,
    borderColor: '#FFDF00',
    paddingHorizontal: 16,
    fontSize: 16,
    color: '#002776',
  },
});